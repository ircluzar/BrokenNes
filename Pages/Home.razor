@* Home page kept minimal intentionally. Root route handled by Nes.razor. *@
@* Minimal placeholder (avoid feature marketing; subject to change). *@

<div class="minimal-home">
    <h1>BrokenNes</h1>
    <p class="small-note">A very normal NES emulator</p>
    <p>
        <a href="/nes" class="btn btn-primary">Launch Emulator</a>
    </p>
</div>

@code {
    // Intentionally empty: keep page lightweight.
}
